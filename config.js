const config = {
  db: {
    host: "localhost",
    user: "root",
    username: "root",
    password: "titanfoller",
    database: "inventory",
    connectTimeout: 60000,
  },
  listPerPage: 10,
};  
module.exports = config;
